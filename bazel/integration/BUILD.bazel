package(default_visibility = ["@npm//@angular/dev-infra-private/visibility:public"])

# Make source files available for distribution via pkg_npm
filegroup(
    name = "files",
    srcs = glob(["*"]) + [
        "@npm//@angular/dev-infra-private/bazel/integration/test_runner:files",
    ],
)
